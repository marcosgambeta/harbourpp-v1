// test with base 16

PROCEDURE Main()

   //? 0x // syntax error
   //? 0X // syntax error
   //? 0xG // syntax error

   ? 0x00
   ? 0x01
   ? 0x02
   ? 0x03
   ? 0x04
   ? 0x05
   ? 0x06
   ? 0x07
   ? 0x08
   ? 0x09
   ? 0x10
   ? 0x0A
   ? 0x0B
   ? 0x0C
   ? 0x0D
   ? 0x0E
   ? 0x0F

   ?

   ? -0x00
   ? -0x01
   ? -0x02
   ? -0x03
   ? -0x04
   ? -0x05
   ? -0x06
   ? -0x07
   ? -0x08
   ? -0x09
   ? -0x10
   ? -0x0A
   ? -0x0B
   ? -0x0C
   ? -0x0D
   ? -0x0E
   ? -0x0F

   ?

   ? 0xFF + 0x01
   ? 0xFF * 2
   ? 0xFF * 3
   ? 0xFF * 4
   ? 0xFF / 2
   ? 0xFF / 3
   ? 0xFF / 4

   WAIT

RETURN
