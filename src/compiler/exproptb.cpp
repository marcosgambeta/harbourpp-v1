// hbexprb.cpp is also included from ../macro/macro.cpp
// However it produces a slightly different code if used in
// macro compiler (there is an additional parameter passed to some functions)
#include "hbexprb.cpp"
